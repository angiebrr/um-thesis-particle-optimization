% !TEX root = .\Thesis.tex

% ================================================================================================================================
% Beginning of Observations
% ================================================================================================================================

\chapter{CHILLISOURCE PARTICLE EFFECT COMPONENT OBSERVATIONS}\label{ch:observations}

% ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
% Overview
% ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
\section{Overview}\label{sec:observe_overview}

The case studies that were performed focused primarily on understanding the contention between the main thread that rendered particles and the background thread that updated them. This chapter will explore various other aspects of \class{Particle Effect Components}. Topics that will be examined include the importance of the \var{ConcurrentParticleData} object, the relationship between ChilliSource's \class{Task Scheduler} and \class{Particle Effect Components}, and the perplexing scenarios in which particle effects do not visually emit particles.

% ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
% Using copies
% ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
\section{Copying Data with ConcurrentParticleData}\label{sec:observe_copies}

% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Overview}\label{ssec:observe_copies_overview}
% ----------------------------------------------------------------------------------------------------------------------------

As discussed in prior chapters, the \var{ConcurrentParticleData} object contains copies of primitives, objects, and data structures needed by the \class{Particle Drawable} to render the particles. It also handles all of the locking necessary in order to synchronize the background and main threads. This section will address what occurs when \var{ConcurrentParticleData} is not used and the member variables within \class{Particle Effect Component} are instead directly used by the \class{Particle Drawable}.

\vspace{-0.3cm}
% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Code}\label{ssec:observe_copies_code}
% ----------------------------------------------------------------------------------------------------------------------------

There are a considerable number of changes that are needed to remove \var{ConcurrentParticleData} from the \class{Particle Effect Component} and \class{Particle Drawable} classes:

\begin{itemize}[nolistsep,topsep=10pt]

	\item Modify the \class{Particle Drawable} to use pointers from the \class{Particle Effect Component} and remove the \var{ConcurrentParticleData} member variable.
	\item Add variables that \var{ConcurrentParticleData} was keeping track of.
	\item Add functions that \var{ConcurrentParticleData} provided. (Which primarily include accessor and locking/unlocking functions)
	\item Modify \func{ParticleUpdateTask} to update member variables from the \class{Particle Effect Component} .
	\item Modify the \class{Particle Effect Component} and \class{Particle Drawable} to utilize locks and mutexes in order to ensure thread safety.

\end{itemize}

The majority of the above changes are described in Code Listings \ref{code:WithoutCPDParticleUpdate} and \ref{code:WithoutCPDParticleRender}, but it is also necessary to expand on some points. 

In addition to keeping track of information needed for rendering, \var{ConcurrentParticleData} has two booleans that the \class{Particle Effect Component} used for scheduling updates: \var{hasActiveParticles} and \var{isUpdating}. These booleans describe whether or not there are any particles active in the effect and if \func{ParticleUpdateTask} is running, respectively. If \var{hasActiveParticles} is false when the component is updating on the main thread, then the component stops\footnotemark $ $. If \var{isUpdating} is false when the component is updating on the main thread, then the component will schedule a \func{ParticleUpdateTask}. In order to replace this functionality from \var{ConcurrentParticleData}, these booleans are added to \class{Particle Effect Component} as atomic booleans that are passed by reference to the \func{ParticleUpdateTask} function.\footnotemark

\addtocounter{footnote}{-1}\footnotetext{The particle effect will only completely stop if \var{looping} is false as well.}
\addtocounter{footnote}{+1}\footnotetext{These booleans are not shown in the pseudocode for simplicity, but their behavior is simple; \var{hasActiveParticles} will switch to false if it did not update any particles in \func{ParticleUpdateTask}, and \var{isUpdating} will switch to false at the end of \func{ParticleUpdateTask}.}

Since the \class{Particle Effect Component} uses the bounding information objects on the main thread and the \class{Drawable} does not, two mutexes will be created for the bounding information and the rest of the rendering data. 

As shown by \clis{code:WithoutCPDParticleUpdate}, the \func{ParticleUpdateTask} is locked by a member variable mutex. It is also passes references to new particle indicies (i.e. \var{copiedAtts.newParticleIndices}) and to the bounding information (i.e. \var{copiedAtts.AABB} and \var{copiedAtts.Sphere}).

\vspace{-0.5cm}
\framecode{C++}{observations}{Copies_WithoutConcurrentParticleData_ParticleUpdateTask.cpp}
{code:WithoutCPDParticleUpdate}
{Pseudocode describing the important changes needed to remove the \var{ConcurrentParticleData} instance from the \class{Particle Effect Component}.}
\vspace{-0.5cm}

\clis{code:WithoutCPDParticleRender} shows that the \class{Particle Drawable} did not change significantly. The most notable change is that the \func{Draw} function is now locked by \class{Particle Effect Component}'s mutex within the \func{Render} function. Additionally, the particles array is not passed to the \func{DrawParticles} function. Instead, the array is passed into the drawable when it is created by the \class{Particle Effect Component}.

\vspace{-0.5cm}
\framecode{C++}{observations}{Copies_WithoutConcurrentParticleData_Render.cpp}
{code:WithoutCPDParticleRender}
{Pseudocode describing the important changes needed to remove the \var{ConcurrentParticleData} instance from the \var{Particle Drawable}.}
\vspace{-0.7cm}

% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Results}\label{ssec:observe_copies_results}
% ----------------------------------------------------------------------------------------------------------------------------

The metrics that are presented are the result of running a series of automated games in four scenarios:

\begin{enumerate}[nolistsep,topsep=0pt]
	\item 1 particle effect, particles range from 0 - 50K, 5K step, 5 5-second runs per game.
	\item 1 particle effect, 50K, 0 step, 20 5-second runs.
	\item 10 particle effects, particles range from 0 - 5K, 500 step, 5 5-second runs per game.
	\item 10 particle effects, 5K, 0 step, 20 5-second runs.
\end{enumerate}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{copies/1PE_0-50K_Render-Ball-Function-Calls}
	{1.0}{1.2}
	{Using Copies Observation: A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Copies1PERenderBallFunctionCalls}

	\addlargesubfigure{copies/1PE_0-50K_Particles-Actually-Rendered}
	{1.0}{1.2}
	{Using Copies Observation: A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Copies1PEParticlesActuallyRendered}

\end{figure}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{copies/10PE_0-5K_Render-Ball-Function-Calls}
	{1.0}{1.2}
	{Using Copies Observation: A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Copies10PERenderBallFunctionCalls}

	\addlargesubfigure{copies/10PE_0-5K_Particles-Actually-Rendered}
	{1.0}{1.2}
	{Using Copies Observation: A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Copies10PEParticlesActuallyRendered}

\end{figure}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{copies/1PE_50K_Section-Calls}
	{1.0}{1.2}
	{Using Copies Observation: A graph showing the number of section calls for various sections of code for a single particle effect.}
	{A graph showing the number of section calls for various sections of code for a single particle effect.}
	{fig:Copies1PESectionCalls}

	\addlargesubfigure{copies/1PE_50K_Timed-Sections}
	{1.0}{1.2}
	{Using Copies Observation: A graph showing the time spent in various sections of code for a single particle effect.}
	{A graph showing the time spent in various sections of code for a single particle effect.}
	{fig:Copies1PETimedSections}

\end{figure}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{copies/10PE_5K_Section-Calls}
	{1.0}{1.2}
	{Using Copies Observation: A graph showing the number of section calls for various sections of code for a single particle effect.}
	{A graph showing the number of section calls for various sections of code for a single particle effect.}
	{fig:Copies10PESectionCalls}

	\addlargesubfigure{copies/10PE_5K_Timed-Sections}
	{1.0}{1.2}
	{Using Copies Observation: A graph showing the time spent in various sections of code for a single particle effect.}
	{A graph showing the time spent in various sections of code for a single particle effect.}
	{fig:Copies10PETimedSections}

\end{figure}

All of these scenarios were ran with PPE values that were 10\% and 100\% of TMP. These eight series of automated games were ran with and without the above changes to the source code, and this totals to 16 series of automated games.

% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Discussion}\label{ssec:observe_copies_discussion}
% ----------------------------------------------------------------------------------------------------------------------------

The \var{particles actually rendered} and \var{render ball function calls} charts for 1 particle effect show something interesting. The "No Object" data series renders the ball fewer times than the original, and it also renders its particles fewer times. An examination of the timing metrics for 1 particle effect shows why this is occurring. Instead of the background thread waiting for the mutex to release in \func{ParticleUpdateTask}, the contention has moved to the \class{Particle Drawable} in the \func{Draw} function. This increase in wait-time disallows the renderer to render as many particles and, as a result, it also takes away rendering time from everything else (including the ball).

The charts for 10 particle effects, however, do not demonstrate this. Both the original and "No Object" data series show nearly identical results. This is because there is less overall contention when the particles are distributed across effects, as discovered in the previous chapter. Although the benefits of using a \var{ConcurrentParticleData} object is not as significant when using many particle effects, it is appealing from a software engineering perspective. Encapsulating the thread synchronization to a single class is clearer and more maintainable, and that will be useful if the developer desires to refactor the \class{ParticleEffectComponent}.


% ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
% Multithreading and task scheduling
% ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
\section{Multithreading and Task Scheduling}\label{sec:observe_threads}

% ----------------------------------------------------------------------------------------------------------------------------
\subsection{The ChilliSource Task Scheduler}\label{ssec:observe_threads_scheduler}
% ----------------------------------------------------------------------------------------------------------------------------

As mentioned earlier in the paper, \func{ParticleUpdateTask} is scheduled as a background thread by ChilliSource's \class{Task Scheduler}. The \class{Task Scheduler} can schedule a variety of task types, and this section will delve into the different outcomes when \func{ParticleUpdateTask} is scheduled as a \idefine{small task}, a \idefine{game logic task}, and if it is not scheduled at all and runs on the main thread.

\vspace{-0.5cm}
\framecode{C++}{observations}{Tasks_TaskScheduler_ScheduleTask.cpp}
{code:TaskSchedulerScheduleTask}
{Pseudocode showing how ChilliSource's \class{Task Scheduler} schedules \idefine{small tasks}, \idefine{game logic tasks}, and \idefine{main thread tasks}.}

A \idefine{small task} is considered to be a task with a short execution time such that it can be processed within a single frame. If a \idefine{small task} runs too long, then the task will have a significantly negative impact on the overall performance of the program. A \idefine{game logic task} is a special kind of small task that is guaranteed to be executed prior \idefine{main thread tasks}. It is easy to see, then, that long running \idefine{game logic tasks} will drastically affect the program's performance. \idefine{Main thread tasks} are also small tasks that are executed on the main thread after \idefine{game logic tasks}. All of these tasks are added to \class{Task Pools}, which are a collection of tasks that are performed by one of the worker threads owned by the pool. These pools will continue to perform tasks until they are deallocated and, if there are no tasks to perform, they will sleep until another task is added.

\vspace{-0.5cm}
\framecode{C++}{observations}{Tasks_TaskScheduler_ExecuteMainThreadTasks.cpp}
{code:TaskSchedulerExecuteMainThread}
{Pseudocode showing how \idefine{game logic tasks} force the main thread to wait until they are finished before allowing the main thread to continue.}



% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Code}\label{ssec:observe_threads_code}
% ----------------------------------------------------------------------------------------------------------------------------

Scheduling the different types of tasks is fairly straightforward; the task type is simply passed into the \func{ScheduleTask} method. This was not included in previous code listings in order to simplify them, but the code listings below illustrate the different ways the \func{ParticleUpdateTask} function was scheduled for this study. It should be noted that, by default, the \class{Particle Effect Component} scheduled \func{ParticleUpdateTask} as a \idefine{small task}. Thus, during this study, the "Small Task" data series is analogous to the "Original" data series in previous sections.

% ...........................................................
\subsubsection{Small Task Type}
% ...........................................................

\vspace{-1.2cm}
\framecode{C++}{observations}{Tasks_Small_ParticleUpdateTask.cpp}
{code:SmallTaskParticleUpdate}
{Pseudocode showing how \func{ParticleUpdateTask} was scheduled as a \idefine{small task}.}

\vspace{-1.2cm}
% ...........................................................
\subsubsection{Game Logic Task Type}
% ...........................................................

\vspace{-1.2cm}
\framecode{C++}{observations}{Tasks_GameLogic_ParticleUpdateTask.cpp}
{code:GameLogicTaskParticleUpdate}
{Pseudocode showing how \func{ParticleUpdateTask} was scheduled as a \idefine{game logic task}.}

\vspace{-1.2cm}
% ...........................................................
\subsubsection{No Task Scheduled (Not Threaded)}
% ...........................................................

The sequential version of \func{ParticleUpdateTask} is performed in a similar fashion to the other tasks in that a struct is passed to it and the \var{ConcurrentParticleData} object is still used. Although these things are not strictly needed, they are included primarily to "level the playing field" so that the results from all three versions are easier to compare.

\vspace{-0.5cm}
\framecode{C++}{observations}{Tasks_NoTask_ParticleUpdateTask.cpp}
{code:GameNoTaskParticleUpdate}
{Pseudocode showing how \func{ParticleUpdateTask} was not scheduled and was simply ran on the main thread.}

% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Results}\label{ssec:observe_threads_results}
% ----------------------------------------------------------------------------------------------------------------------------

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{task_scheduling/1PE_0-50K_Render-Ball-Function-Calls}
	{1.0}{1.2}
	{Task Scheduling Observation: A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Tasks1PERenderBallFunctionCalls}

	\addlargesubfigure{task_scheduling/1PE_0-50K_Particles-Actually-Rendered}
	{1.0}{1.2}
	{Task Scheduling Observation: A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Tasks1PEParticlesActuallyRendered}

\end{figure}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{task_scheduling/10PE_0-5K_Render-Ball-Function-Calls}
	{1.0}{1.2}
	{Task Scheduling Observation: A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Tasks10PERenderBallFunctionCalls}

	\addlargesubfigure{task_scheduling/10PE_0-5K_Particles-Actually-Rendered}
	{1.0}{1.2}
	{Task Scheduling Observation: A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Tasks10PEParticlesActuallyRendered}

\end{figure}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{task_scheduling/1PE_50K_Section-Calls}
	{1.0}{1.2}
	{Task Scheduling Observation: A graph showing the number of section calls for various sections of code for a single particle effect.}
	{A graph showing the number of section calls for various sections of code for a single particle effect.}
	{fig:Tasks1PESectionCalls}

	\addlargesubfigure{task_scheduling/1PE_50K_Timed-Sections}
	{1.0}{1.2}
	{Task Scheduling Observation: A graph showing the time spent in various sections of code for a single particle effect.}
	{A graph showing the time spent in various sections of code for a single particle effect.}
	{fig:Tasks1PETimedSections}

\end{figure}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{task_scheduling/10PE_5K_Section-Calls}
	{1.0}{1.2}
	{Task Scheduling Observation: A graph showing the number of section calls for various sections of code for a single particle effect.}
	{A graph showing the number of section calls for various sections of code for a single particle effect.}
	{fig:Tasks10PESectionCalls}

	\addlargesubfigure{task_scheduling/10PE_5K_Timed-Sections}
	{1.0}{1.2}
	{Task Scheduling Observation: A graph showing the time spent in various sections of code for a single particle effect.}
	{A graph showing the time spent in various sections of code for a single particle effect.}
	{fig:Tasks10PETimedSections}

\end{figure}

\newpage
% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Discussion}\label{ssec:observe_threads_discussion}
% ----------------------------------------------------------------------------------------------------------------------------

LOREM LOREM

% ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
% Taxing the system and emission failure
% ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
\section{Taxing the Engine and Emission Failure}\label{sec:observe_emit_failure}

% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Overview}\label{ssec:observe_emit_failure_overview}
% ----------------------------------------------------------------------------------------------------------------------------

LOREM LOREM

% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Emission Failure}\label{ssec:observe_emit_failure_failure}
% ----------------------------------------------------------------------------------------------------------------------------

LOREM LOREM

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{emission_failure/1PE_0-50K_Render-Ball-Function-Calls}
	{1.0}{1.2}
	{Emission Failure Observation: A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Failure1PERenderBallFunctionCalls}

	\addlargesubfigure{emission_failure/1PE_0-50K_Particles-Actually-Rendered}
	{1.0}{1.2}
	{Emission Failure Observation: A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Failure1PEParticlesActuallyRendered}

\end{figure}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{emission_failure/10PE_0-5K_Render-Ball-Function-Calls}
	{1.0}{1.2}
	{Emission Failure Observation: A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Failure10PERenderBallFunctionCalls}

	\addlargesubfigure{emission_failure/10PE_0-5K_Particles-Actually-Rendered}
	{1.0}{1.2}
	{Emission Failure Observation: A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{fig:Failure10PEParticlesActuallyRendered}

\end{figure}

\newpage
% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Emission Failure Source}\label{ssec:observe_emit_failure_source}
% ----------------------------------------------------------------------------------------------------------------------------

LOREM LOREM

% ----------------------------------------------------------------------------------------------------------------------------
\subsection{Emission Failure Solution}\label{ssec:observe_emit_failure_solution}
% ----------------------------------------------------------------------------------------------------------------------------

LOREM LOREM

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{emission_failure/solution/1PE_0-50K_Render-Ball-Function-Calls}
	{1.0}{1.2}
	{Emission Failure Solution Observation: A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{fig:FailureSolution1PERenderBallFunctionCalls}

	\addlargesubfigure{emission_failure/solution/1PE_0-50K_Particles-Actually-Rendered}
	{1.0}{1.2}
	{Emission Failure Solution Observation: A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{fig:FailureSolution1PEParticlesActuallyRendered}

\end{figure}

\begin{figure}
\addtocounter{figure}{-1}

	\addlargesubfigure{emission_failure/solution/10PE_0-5K_Render-Ball-Function-Calls}
	{1.0}{1.2}
	{Emission Failure Solution Observation: A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{render ball function calls} over \idefine{total maximum particles} for a single particle effect.}
	{fig:FailureSolution10PERenderBallFunctionCalls}

	\addlargesubfigure{emission_failure/solution/10PE_0-5K_Particles-Actually-Rendered}
	{1.0}{1.2}
	{Emission Failure Solution Observation: A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{A graph showing \idefine{particles actually rendered} over \idefine{total maximum particles} for a single particle effect.}
	{fig:FailureSolution10PEParticlesActuallyRendered}

\end{figure}

% ================================================================================================================================
% End of Observations
% ================================================================================================================================

